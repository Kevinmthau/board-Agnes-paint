{
  "name": "paint-board",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal web game scaffold demonstrating the Board Web SDK.",
  "scripts": {
    "dev": "vite",
    "test": "esbuild test/glyphFrame.test.ts --bundle --platform=node --format=esm --outfile=.tmp-test/glyphFrame.test.mjs && node --test .tmp-test/glyphFrame.test.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@harrishill/board-sdk": "file:../vendor/harrishill-board-sdk-0.1.0.tgz"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.4.0",
    "vite": "^6.4.2"
  }
}
